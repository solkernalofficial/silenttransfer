# Root convenience template (optional).
# Prefer apps/api/.env.example and apps/web/.env.example for local development.
# NEVER put real private keys or production secrets in committed files.

# See:
#   apps/api/.env.example
#   apps/web/.env.example
#   contracts/.env.example
